file='dados\english.txt
[filepath,name,ext] = fileparts('C:\Dev\TI_Project1LNG\dados\english.txt');

if(ext == '.txt')
    id = fopen('dados\english.txt', 'r');
    inf = fread(id);
elseif(ext == '.wav')
    id= audioread(filepath);
    inf= audioinfo(filepath);
elseif(ext== '.bmp')
   % id=
    %inf=
end
    
    hist=drawhist(id, inf);
    disp('Entropia de %s: %f\n', name, entropia(hist));